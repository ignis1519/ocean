description: mongo-atlas integration for Port Ocean
icon: mongodb
features:
  - type: exporter
    section: Under Development
    resources:
      - kind: mongo-atlas-cluster
configurations:
  - name: mongoAtlasClientId
    description: Client ID for MongoDB Atlas API access.
    required: true
    type: string
  - name: mongoAtlasClientSecret
    description: Client secret for MongoDB Atlas API access.
    required: true
    type: string
